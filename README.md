# Webdev
Because HTML and CSS together unlocks creative freedom
#STEPS TO WORK WITH THIS
1. clone the repo in the VS code.
2. Run the local python3 server in the VS code.

From your project folder:

python3 -m http.server 8000

3. Click open in the browser in the VS code pop up window.

